using System;
using System.Collections.Generic;
using System.Reflection;
using System.Text;

namespace System.CommandLine.Invocation
{
    public static class ReflectionExtensions
    {
        //public static Binding Create(MethodInfoWrapper methodWrapper, ParameterInfo parameterInfo)
        //    => new Binding((context, target, value) => methodWrapper.Assign(parameterInfo, value));

        //public static Binding Create(PropertyInfo propertyInfo)
        //     => new Binding((context, target, value) => propertyInfo.SetValue(target, value));
    }
}
